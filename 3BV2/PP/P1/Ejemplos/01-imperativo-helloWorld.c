#include <stdio.h> // biblioteca

// definimos nuestra funcion inicial ue va a ejecutar todo nuestro codigo
int main () {
	printf("Texto");
	return 0; // valor para indicar que el programa se ejecuto sin errores
}

// decimos que C es eun lenguaje imperativo porque le indicamos paso a paso lo que se requiere para poder llevar a cabo la aejecucion del programa
